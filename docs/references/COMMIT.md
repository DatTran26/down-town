# Commit dialect

- If runtime has ak:git: follow ak:git commit-standards (conventional, <72, no AI trailer).
- Else: caveman-commit (≤50, no emoji, no AI trailer) + principled-git-commit (atomic + secrets).
- English subject. Why over what when not using ak:git.
